\lst@definelanguage{eventb}
  {
    morekeywords={alias,all,and,as,begin,check,class,interface,
      creation,create,debug,deferred,do,else,elseif,end,
      ensure,expanded,export,external,false,feature,from,frozen,if,
      implies,indexing,infix,inherit,inspect,invariant,confined,
      like,local,loop,not,obsolete,old,once,or,prefix,
      redefine,rename,require,rescue,retry,select,when,while,
      separate,strip,then,true,undefine,unique,until,variant,use,modify,
      when, xor, Result, Current, Void, True, False, is},
    sensitive=false,
    showstringspaces=false,
    morecomment=[l]--,
    morestring=[d]",
    columns=fullflexible,
    breaklines=true,
    tabsize=3,
    identifierstyle=\itshape,
    commentstyle=\upshape,
    stringstyle=\bfseries,
    %basicstyle=\small,
    basicstyle=\footnotesize,
    keywordstyle=\bfseries,
  }[keywords,comments,strings]


\lst@definelanguage{OOSC2Eiffel}
  {
    morekeywords={alias,all,and,as,check,class,interface,
      creation,create,debug,deferred,do,else,elseif,end,
      ensure,expanded,export,external,false,feature,from,frozen,if,
      implies,indexing,infix,inherit,inspect,invariant,confined,
      like,local,loop,not,obsolete,old,once,or,prefix,
      redefine,rename,require,rescue,retry,select,
      separate,strip,then,true,undefine,unique,until,variant,use,modify,
      when, xor, Result, Current, Void, True, False},
    sensitive=false,
    showstringspaces=false,
    morecomment=[l]--,
    morestring=[d]",
    columns=fullflexible,
    breaklines=true,
    tabsize=3,
    identifierstyle=\itshape,
    commentstyle=\upshape,
    stringstyle=\bfseries,
    %basicstyle=\small,
    basicstyle=\footnotesize,
    keywordstyle=\bfseries,
  }[keywords,comments,strings]



\lst@definelanguage{pvs}
{morekeywords={DATATYPE,THEORY,FORALL,BEGIN,THEOREM,CONJECTURE,LEMMA,EXISTS,IF,ELSE,ELSIF,THEN,ENDIF,TABLE,ENDTABLE,COND,ENDCOND,IMPLIES,=>,LEMMA,LET,IN,RECURSIVE,MEASURE,DO,TO,BY,FOR,END_FOR,VAR_INPUT,VAR_OUTPUT,VAR,END_VAR,TYPE,TRUE,FALSE,IMPORTING,END,AND,OR,NOT,BOOL},
sensitive=false,
morecomment=[l]{\%},
morecomment=[s]{/*}{*/},
morestring=[b]",
keywordstyle=\bfseries
}[keywords, comments, strings]